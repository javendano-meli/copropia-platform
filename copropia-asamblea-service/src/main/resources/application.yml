server:
  port: 8083

spring:
  application:
    name: asamblea-service
  datasource:
    url: jdbc:postgresql://localhost:5435/copropia_asamblea
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
